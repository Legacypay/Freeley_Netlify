# Freeley — environment variables template. Copy to .env (gitignored) and
# fill in real values. No secrets belong in this file — it's committed.
# Named env.example.txt (not .env.example) because dotfiles matching
# ".env*" are blocked by this project's write-permission guard.

# ── Supabase ─────────────────────────────────────────────────────────────
# This project's ONLY auth/database system — Firebase is not used and
# never will be (client decision). Covers both funnel_leads capture and
# the ported patient-hub functions' token verification
# (netlify/functions/lib/verify-supabase-token.cjs).
# Safe to expose client-side: publishable/anon key only, protected by RLS
# with zero anon table policies on funnel_leads — the save_funnel_lead()
# RPC is the only write path. See supabase/migrations/0001_funnel_leads.sql.
PUBLIC_SUPABASE_URL=https://kqstjyyyzegfineglgug.supabase.co
PUBLIC_SUPABASE_ANON_KEY=sb_publishable_rsi1qFD_dPbJFJLnoopG0w_ROV-RXxR

# ── MD Integrations (telemedicine partner) ──────────────────────────────────
# Full submission + patient-hub flow ported into netlify/functions/
# (submitQuiz.cjs, mdiWebhook.cjs, savePendingCase.cjs,
# retryPendingCases.cjs, caseStatus.cjs, getMessagingAuth.cjs,
# patientCases.cjs, getEncounterDetails.cjs) — read the CODE for exactly
# which vars each function needs, this list is pulled straight from
# netlify/functions/lib/mdi-client.cjs and phi-crypto.cjs, not guessed.
# See netlify/functions/TODO-SUPABASE.md for what's still deferred.
# Get real values from whoever holds the MDI partner portal credentials —
# never invent placeholder-looking values that could be mistaken for real.
# MDI_CLIENT_ID=
# MDI_CLIENT_SECRET=
# MDI_WEBHOOK_SECRET=
# MDI_BASE_URL=https://api.mdintegrations.com
# MDI_PARTNER_ID=f81508d1-3c53-4849-a636-1e9050a68e00
# MDI_AUTH_SCOPE=*
# MDI_LIVE_MODE=false

# PHI encryption at rest (netlify/functions/lib/phi-crypto.cjs) — required
# by savePendingCase.cjs / retryPendingCases.cjs, which persist patient
# data to Netlify Blobs while a failed MDI submission awaits retry.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# PHI_ENCRYPTION_KEY=

# Internal alerts (submitQuiz/mdiWebhook/savePendingCase/retryPendingCases
# all fire here on failures, status changes, and permanent-failure alerts)
# N8N_WEBHOOK_URL=

# ── The following are NOT wired into this repo, and not part of the MDI
# port — they belong to the separate production backend at
# github.com/Legacypay/Freeley_Netlify. Listed only so the names are
# documented in one place if that work happens later.

# Stripe (payment — separate decision, see netlify/functions/TODO-SUPABASE.md)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=

# Analytics
# SLACK_WEBHOOK_URL=
# META_PIXEL_ID=
# META_ACCESS_TOKEN=
# GA4_MEASUREMENT_ID=
# GA4_API_SECRET=
